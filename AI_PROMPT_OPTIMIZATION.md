# AI 提示词优化说明

## 📋 优化内容

### 1. 生成内容质量提升

**之前：** 简单摘要（150字以内）
```
请用中文总结以下文章（不超过150字）：
标题：...
内容：...
```

**现在：** 专业资讯报道（200-300字）
```
请将以下 AI 资讯改写成一篇专业的科技报道（200-300字）：

写作要求：
1. 开头：一句话概括核心要点
2. 主体：详细说明技术细节、产品特点，用数据支撑
3. 分析：对行业/用户的实际影响
4. 格式：Markdown，关键词加粗，emoji 标注多媒体
5. 结尾：保留原文链接 📎 [查看原文](...)
```

### 2. 内容结构优化

**标准报道结构：**
```markdown
**核心要点概括**（What/Why）

详细技术说明，包含具体数据和例子。特别标注 **关键词**。📊

影响分析和行业意义。

📎 [查看原文](https://example.com)
```

### 3. 多媒体内容支持

- ✅ 自动识别图片链接（`images` 字段）
- ✅ 自动识别视频链接（`videos` 字段）
- ✅ 在报道中用 emoji 标注（📊 🎬 🖼️）

### 4. HTML 报告优化

- ✅ 支持 Markdown 渲染（粗体、链接、段落）
- ✅ 响应式设计，渐变背景
- ✅ 悬停效果和交互动画
- ✅ 更清晰的排版和视觉层次

## 🚀 使用方法

### 步骤1：数据库迁移（如需支持图片/视频字段）

在 Supabase SQL Editor 中执行：

```sql
-- 添加图片和视频字段
ALTER TABLE articles 
ADD COLUMN IF NOT EXISTS images TEXT[] DEFAULT '{}',
ADD COLUMN IF NOT EXISTS videos TEXT[] DEFAULT '{}';

-- 添加索引
CREATE INDEX IF NOT EXISTS idx_articles_images ON articles USING GIN (images);
CREATE INDEX IF NOT EXISTS idx_articles_videos ON articles USING GIN (videos);
```

### 步骤2：重启开发服务器

```powershell
cd apps\web
npm run dev
```

### 步骤3：生成日报

1. 访问 http://localhost:3000
2. 勾选需要的文章
3. 点击"生成日报"
4. AI 会自动生成专业报道格式的内容

## 📊 效果对比

### 旧版摘要
> OpenAI 发布了新的 GPT-5 模型，性能有显著提升，支持更长的上下文。

### 新版报道
> **OpenAI 发布 GPT-5**，性能相比 GPT-4 提升 40%，推理速度快 2 倍。
> 
> 新模型支持 **128K 上下文长度**，可处理约 100 页文档。测试显示在数学推理、代码生成等任务上表现更优，特别是复杂问题分解能力显著增强。📊
> 
> 这将显著降低企业 AI 应用成本，加速多模态 AI 落地。OpenAI 计划于 12 月向 API 用户开放。
> 
> 📎 [查看原文](https://openai.com/gpt5)

## 🎯 AI 提供商配置

当前使用 **DeepSeek**（`AI_PROVIDER=deepseek`）

- ✅ 国内访问无障碍
- ✅ 价格便宜（约 Gemini 的 1/10）
- ✅ 生成质量优秀

如需切换到 Gemini：
```env
AI_PROVIDER=gemini
GEMINI_API_KEY=your_key_here
```

## 📝 自定义提示词

编辑以下文件自定义 AI 生成风格：

- **DeepSeek**: `packages/ai/src/providers/deepseek.ts`
- **Gemini**: `packages/ai/src/providers/gemini.ts`

关键参数：
```typescript
{
  temperature: 0.7,      // 创造性（0-1，越高越有创意）
  max_tokens: 800,       // 最大生成长度
  // ... 提示词模板
}
```

## 🔧 技术细节

### 文件修改清单

1. `packages/ai/src/types.ts` - 添加 `images` 和 `videos` 字段
2. `packages/ai/src/providers/deepseek.ts` - 优化提示词
3. `packages/ai/src/providers/gemini.ts` - 优化提示词，增加 max_tokens
4. `packages/processors/src/index.ts` - 优化 HTML 模板，支持 Markdown
5. `packages/db/src/types.ts` - Article 接口添加多媒体字段

### 新增功能

- ✅ Markdown 转 HTML（简单版）
- ✅ 多媒体内容标注
- ✅ 原文链接强制保留
- ✅ 响应式 HTML 模板
- ✅ 渐变背景和动画效果

## 🤔 常见问题

**Q: 生成的内容还是太短？**
A: 调整 `max_tokens` 参数（当前 800）

**Q: 想要更简洁的风格？**
A: 修改提示词中的"200-300字"为"150-200字"

**Q: 如何提取文章中的图片？**
A: 需要在 `fetchers` 中增加 HTML 解析逻辑（如使用 cheerio 库）

**Q: 生成速度慢？**
A: DeepSeek 通常 2-5 秒/篇，如需加速可减少 `max_tokens`

## 📚 相关文档

- [NETWORK_ISSUES.md](./NETWORK_ISSUES.md) - 网络连接问题解决方案
- [GEMINI_API_FIX.md](./GEMINI_API_FIX.md) - Gemini API 配置说明
- [README.md](./README.md) - 项目完整文档
